# Roadmap por fases

Regra fundamental: **não avançar para uma fase sem verificar se a anterior está funcional.** Cada fase é uma resposta explicando objetivo/arquivos/arquitetura/dependências/fluxo de dados **antes** de implementar (ver processo de desenvolvimento abaixo).

```
FASE 0  → Arquitetura                        [CONCLUÍDA]
FASE 1  → Backend básico (Phoenix, health check)                        [CONCLUÍDA — v0.1.0]
FASE 2  → Autenticação (Guardian, argon2id, guardian_db)                [CONCLUÍDA — v0.2.0]
FASE 3  → Usuários e servidores (roles, permissões, convites, bans)     [CONCLUÍDA — v0.3.0]
FASE 4  → Chat em tempo real (núcleo — anexos leves pendentes)          [NÚCLEO CONCLUÍDO — v0.4.0-core]
FASE 5  → Canais (categorias, permissões por canal)                    [CONCLUÍDA — v0.5.0]
FASE 6  → Voz (mesh WebRTC, sinalização, Presence)                     [CONCLUÍDA — v0.6.0]
FASE 7  → Vídeo (câmera, MediaSession, cap de 4 participantes)         [CONCLUÍDA — v0.7.0]
FASE 8  → Compartilhamento de tela (desktopCapturer)                   [CONCLUÍDA — v0.8.0, cap revisado após v0.15.0]
──────────────────────────────────────────── fim do MVP (backend) ────
        → Amigos / Mensagens Diretas (schema já pronto na FASE 3/4)     [FORA DE ESCOPO — ver item 8]
FASE 9  → Go Live (SFU: LiveKit — ver media.md)                        [CONCLUÍDA (backend) — v0.9.0]
FASE 10 → Arquivos — gestão avançada (redefinida, ver nota abaixo)     [FORA DE ESCOPO — ver item 8]
FASE 11 → Cliente Electron (empacotamento, distribuição)          [CONCLUÍDA — v0.17.0]
FASE 12 → Rust (media services — redirecionado pela adoção de SFU na FASE 9, ver media.md)
FASE 13 → C++ (media engine nativo, chamado a partir do Rust)
FASE 14 → Python / Machine Learning (analytics, moderação automática)
FASE 15 → Escalabilidade (libcluster, Redis PubSub adapter, SFU independente)
FASE 16 → Segurança (hardening de produção, gestão de segredos, TLS real no coturn)
FASE 17 → SDK (Rust primeiro, depois bindings)
FASE 18 → Testes e produção                    [EM ANDAMENTO — deploy em VPS via Dokploy, v1.7.0]
```

**"MVP (backend)" concluído** com a FASE 8 (`v0.8.0`): 68 testes passando, cada
fase verificada de ponta a ponta (incluindo com clientes WebSocket reais, não
só a suíte automatizada). O qualificador "(backend)" importa — FASES 6, 7 e 8
implementaram toda a sinalização/autorização/Presence do lado do servidor,
mas a metade client-side (peer connections WebRTC de verdade, perfect
negotiation, STUN/coturn, `desktopCapturer`) depende do Electron e só entra
na FASE 11. Sem um cliente de verdade, ninguém liga microfone/câmera/tela
ainda — só o "esqueleto" que torna isso possível está pronto.

Pendente antes de um MVP completo de ponta a ponta: anexos leves (FASE 4,
precisa de Docker/MinIO) e verificação de mídia real do Go Live contra um
LiveKit rodando (FASE 9, também precisa de Docker). Amigos/DM segue fora de
escopo por decisão do usuário (fast-follow abaixo, não planejado). A FASE 11
(cliente Electron, incluindo empacotamento) está **concluída** (`v0.16.0`).

## Correções registradas em relação ao documento original

1. **Anexos de arquivo**: o documento original lista "Uploads" como FASE 10 (depois do Go Live). Anexos **leves** (imagem/arquivo pequeno, com validação) entram na **FASE 4** — chat sem nenhum anexo não é utilizável, e isso evita que a FASE 4 fique "funcional" apenas parcialmente. A FASE 10 passa a significar **gestão avançada de arquivos** (arquivos grandes, upload retomável, cotas, CDN) — um endurecimento pós-MVP, não a introdução do recurso básico.
2. **Amigos/DM**: não aparecia como fase explícita no documento original (schema de `server`/`channel` sugeria isso implicitamente). Fica registrado aqui como **fast-follow logo após a FASE 8** (fora do MVP formal, mas antes da FASE 9), já que o schema (`friend_requests`, `channels.type = dm/group_dm`, `channel_recipients`) é desenhado desde a FASE 3/4 para suportar isso sem retrabalho. **Fora de escopo pra este deploy** (ver item 8) — o schema continua pronto se um dia fizer sentido, mas não é trabalho planejado.
3. **FASE 9 (Go Live)**: decisão de SFU resolvida no kickoff da fase — **LiveKit**, não mediasoup (a recomendação preliminar original) — ver `media.md` para o raciocínio completo. Backend (autorização + emissão de token) implementado e verificado; o lado cliente/mídia real depende do LiveKit rodando (sem Docker neste ambiente) e do Electron (FASE 11).
4. **FASE 11 (Electron)** é grande demais pra uma fatia só (Regra 1) — sendo dividida em fatias dentro da própria fase, cada uma com seu próprio ciclo explicar → implementar → testar: **fatia 1** (scaffold + autenticação, `v0.10.0`), **fatia 2** (lista de servidores/canais — navegação só leitura, `v0.11.0`, trouxe também o `GET /api/v1/servers` que faltava no backend), **fatia 3** (chat em tempo real — socket, histórico + `message:create` ao vivo, `v0.12.0`), **fatia 4** (voz — mesh WebRTC de verdade com perfect negotiation, `v0.13.0`), **fatia 5** (compartilhamento de tela — `desktopCapturer`, `v0.14.0`, ver `media.md`), **fatia 6** (vídeo/câmera — `v0.15.0`, ver `media.md`), **fatia 7** (deafen — `v0.15.2`, ver `media.md`), **fatia 8** (editar/apagar/reagir mensagem — `v0.15.3`, ver `realtime.md`) e **fatia 9** (criar canal — `v0.15.4`) concluídas; entre a 5 e a 6, o fluxo de autenticação foi simplificado a pedido do usuário (cadastro só usuário+senha, login por `username#discriminator`+senha como caminho de recuperação — sessão persiste sozinha depois do primeiro login, `v0.14.1`, ver `api.md`/`database.md`/`security.md`); logo após a 6, o cap de tela foi revisado (item 7 abaixo, `v0.15.1`). A fatia 8 também expôs `message:reaction:remove` no Channel, evento novo — `Chat.remove_reaction/3` já existia no contexto desde a FASE 4 mas nunca tinha sido exposto. A fatia 9 não mudou nada no backend (`POST /servers/:id/channels` já existia e já era testado desde a FASE 4/5) — só ligou o cliente: form inline na `ChannelList` (nome + tipo texto/voz), qualquer membro vê o botão, servidor sempre revalida `manage_channels` (sem checagem de permissão replicada no cliente, mesmo padrão de vídeo/tela). **Fatia 10** (Go Live, `v0.15.5`) também não mudou o backend (FASE 9 já estava pronta) — `stores/goLiveStore.ts` novo (`livekit-client` oficial) + `VoicePanel.tsx` integrando os controles; achou e corrigiu um bug real de CSP que bloqueava qualquer conexão ao LiveKit (ver item 9 abaixo e `docs/media.md`). **Fatia 11** (empacotamento/distribuição, `v0.16.0`, encerra a FASE 11 inteira) — `electron-builder` (`^26.15.3`) gera um instalador NSIS real (`desktop/electron-builder.yml`); ícone próprio criado do zero (`desktop/build/icon.ico`/`icon.png`, não existia nenhum antes). Só Windows por enquanto — é a única plataforma testável neste ambiente e a que os ~20 usuários do deploy de fato usam; macOS/Linux ficam pra quando (e se) fizer sentido, é só estender o mesmo `electron-builder.yml` com blocos `mac`/`linux`. Sem assinatura de código (precisa de certificado pago, fora de escopo) e sem auto-update (não pedido, mecanismo de publish do electron-builder nem foi configurado) — instalador roda localmente, sem depender de conta/servidor nenhum, e o Windows vai mostrar aviso de "editor desconhecido" no primeiro uso (SmartScreen), o que é esperado pra um instalador não assinado. **Fatia 12** (`v0.17.0`, não planejada originalmente — descoberta ao criar o servidor único de verdade pra esse deploy): faltava qualquer jeito de **entrar** num servidor existente pelo cliente. "Criar servidor" sempre esteve fora de escopo (item 8 abaixo), mas "entrar via convite" nunca tinha sido discutido nem construído em nenhuma fatia anterior — só apareceu como lacuna real ao tentar efetivamente colocar as ~20 pessoas dentro do servidor "TORA DOS BURRO" (criado direto via API, dono = conta real do usuário, token gerado pelo próprio código de auth do backend sem tocar em senha nenhuma). Backend não mudou (`GET /invites/:code` e `POST /invites/:code/join` já existiam e já eram testados desde a FASE 3) — só ligou o cliente: botão "+" verde no `ServerSidebar` (ao lado dos ícones de servidor) abre um modal (`JoinServerDialog.tsx`, no mesmo estilo do `ScreenSharePicker.tsx`) com um campo pro código; erros do backend (`not found`/`expired`/`max_uses_reached`/`banned`/`already_member`) traduzidos pra mensagens claras, mesmo padrão do 403 de criar canal.
5. **Bug de build corrigido na fatia 5, mas existia desde a fatia 1**: preload sandboxado (`sandbox: true`) não aceita ESM — precisa compilar como CommonJS (`output: { format: 'cjs' }`). Sem isso, `window.api` nunca existia de verdade dentro do Electron real (só parecia "esperado" estar ausente porque toda verificação anterior rodava numa aba de navegador comum, sem preload nenhum). Achado e corrigido só na fatia 5 porque foi a primeira vez que a verificação rodou contra a janela Electron de verdade (via CDP/`--remoteDebuggingPort`) em vez do browser pane. Ver `docs/media.md` e `docs/security.md` para os detalhes.
6. **Dois bugs reais de WebRTC corrigidos na fatia 6** (câmera): um sobre criação antecipada de transceivers colidindo com *glare* na negociação inicial, outro (mais sutil) sobre `presence.onLeave` derrubando peer connections ativas por causa de uma corrida assíncrona no `Phoenix.Presence.handle_diff/2` — nenhum dos dois apareceu nas fatias 4/5 porque só se manifestam com múltiplas tracks de vídeo trocando de estado em sequência. Ver `docs/media.md` (seção "Fluxo de vídeo") para a causa raiz completa de cada um.
7. **FASE 8 revisada logo após a fatia 6**: limite de 1 compartilhamento de tela por vez removido (`v0.15.1`) a pedido explícito do usuário, que definiu o escopo real de deploy deste app — **um único servidor, uso privado por ~20 pessoas conhecidas, nunca público**. O motivo original do limite (cada compartilhamento simultâneo é mais uma faixa de vídeo que todo peer do mesh precisa decodificar) continua tecnicamente válido e não foi resolvido, só aceito conscientemente dado esse escopo — não é uma mudança segura de generalizar para um deploy público ou salas maiores. O cap de vídeo por câmera (4 simultâneos) foi mantido como estava — a revisão foi só pra compartilhamento de tela. Ver `docs/media.md` ("Limites do mesh").
8. **Escopo do deploy confirmado pelo usuário após a fatia 6**: um único servidor, uso privado por ~20 pessoas conhecidas, nunca público (mesma decisão do item 7). Consequências registradas aqui: **Amigos/DM** (item 2) e **FASE 10** (gestão avançada de arquivos, item 1) saem do que está planejado — sem múltiplos servidores, DM entre servidores diferentes não faz sentido, e sem usuários desconhecidos/externos, os endurecimentos de FASE 10 (cotas, CDN, upload retomável em escala) não têm motivo. **"Criar servidor" no cliente também sai de escopo** — o servidor único já existe, criado direto via API; "criar canal" (dentro dele) continua como trabalho válido, é um recurso menor e independente. Anexos **leves** (FASE 4, ainda pendente — precisa de Docker/MinIO) não foram excluídos, só a versão avançada (FASE 10).
9. **Bug real de CSP corrigido na fatia 10 (Go Live)**: a `Content-Security-Policy` do cliente (`desktop/src/renderer/index.html`) nunca liberava `connect-src` para a URL do LiveKit — nenhum host, nem um esquema genérico — então `room.connect()` do `livekit-client` falhava **sempre**, em qualquer ambiente (não só aqui, sem Docker), com uma mensagem genérica indistinguível de "servidor fora do ar". Só apareceu inspecionando o console do DevTools ao vivo (evento `securitypolicyviolation`), não em `npm run typecheck` nem lendo o código — a própria razão de o processo exigir teste ao vivo, não só checagem estática. Como a URL do LiveKit é dinâmica (entregue pelo backend em runtime via `LIVEKIT_URL`, nunca fixa no build do cliente), a correção libera esquemas (`ws:`/`wss:`/`http:`/`https:` para o `:7880` local + `wss:`/`https:` genéricos para produção), não um host específico. Ver `docs/media.md` (seção "Fluxo do Go Live") para o detalhe completo, incluindo o segundo bloqueio (preflight HTTP `.../validate`) descoberto ao corrigir o primeiro.
10. **FASE 18 iniciada informalmente: primeiro deploy real numa VPS (Dokploy), `v1.0.1`**. Backend rodando de verdade em `http://2.28.229.48` (Docker/Dokploy, sem domínio/TLS ainda — decisão explícita pra essa primeira subida) com Postgres gerenciado; cliente reconstruído apontando pra lá (`desktop/.env.production`, novo). **Três bugs reais só visíveis num deploy de verdade** (nenhum apareceu em nenhum ambiente anterior, incluindo os `mix test`/`npm run typecheck` de todas as fases):
    - `rel/overlays/bin/{server,migrate}` (gerados por `mix phx.gen.release --docker`, fatia 11) foram commitados sem o bit de execução Unix — gerados no Windows, onde esse conceito nem existe. O container falhava a cada tentativa com `permission denied` num `exec` que nunca chega a rodar nenhuma linha do BEAM, então nenhum log da aplicação aparecia — só o log de infraestrutura do próprio Docker Swarm (achado trocando os logs do Dokploy de "Native" pra "Swarm"). Corrigido com `git update-index --chmod=+x`.
    - `config/prod.exs` tinha `force_ssl` ligado desde o scaffold original da FASE 1, nunca revisitado — todo pedido HTTP virava 301 pra um `https://` que não existe nesse deploy sem TLS. Corrigido excluindo o IP da lista `exclude: [hosts: [...]]` que já existia (mesmo mecanismo usado pra localhost/127.0.0.1), não desligando `force_ssl` inteiro.
    - O servidor "TORA DOS BURRO" + convite criados antes (fatia 12, ao testar "entrar em servidor") tinham sido criados no Postgres **local** de desenvolvimento, não no Postgres novo da VPS — bancos completamente separados. O cliente apontando pra produção não via nada disso; só ficou claro ao ver, pelo histórico de rede do próprio cliente instalado, que um convite antigo respondia "não encontrado". Recriados direto contra a produção.
    - Também descoberto: já existia um projeto "TORA" nesse mesmo Dokploy, de uma tentativa anterior separada (repositório "Discord2", não `personal-communication` — esse repositório se chamava `tora-dos-burro` até pouco depois da v1.7.0, quando foi renomeado) — deixado intocado, o deploy novo foi pra um projeto Dokploy à parte.
11. **coturn (TURN) implantado na VPS**, como serviço Dokploy Compose separado (`coturn`, Compose Type **"Docker Compose"**, não "Stack"/Swarm — resolve de vez a dúvida da nota anterior: rodando via `docker compose` puro, `network_mode: host` funciona exatamente igual ao dev local, sem nenhuma das limitações de rede do Swarm para a faixa larga de portas UDP que o TURN precisa). Fonte: `docker/coturn/docker-compose.prod.yml` + `docker/coturn/turnserver.prod.conf` (ambos commitados, sem segredo nenhum neles — `TURN_SECRET` só existe na aba Environment desse serviço no Dokploy, igual ao do `backend`).
    - **Quarto bug real só visível no deploy**: a primeira tentativa passou toda a configuração como flags soltas de linha de comando (`-n` + uma lista longa de flags, sem arquivo de config montado). O container entrou em loop de reinício (exit 255) — o `docker-entrypoint.sh` da imagem oficial faz `exec turnserver` com esses argumentos, e algum deles não bateu com o que o `getopt_long` do turnserver esperava, fazendo ele despejar o próprio texto de `--help` inteiro e sair, sempre, a cada reinício. Corrigido movendo quase tudo pra um `turnserver.prod.conf` montado (mesmas diretivas já usadas sem problema em dev desde a FASE 6, passando pelo parser de arquivo, não pelo de CLI) e reduzindo a superfície de CLI a um único override: `-c <path> --static-auth-secret=${TURN_SECRET}`.
    - **Verificado de verdade, não só "container rodando"**: um STUN Binding Request (RFC 5389) manual confirmou o processo respondendo na porta pública; um handshake completo de TURN Allocate com credencial de longo prazo (mesmo algoritmo de `ToraDosBurro.Turn` — usuário/senha efêmeros via HMAC, chave MD5(user:realm:senha), MESSAGE-INTEGRITY) confirmou autenticação E alocação de relay funcionando (`2.28.229.48:51411`, dentro da faixa default 49152-65535 do RFC 5766) — contra o IP público real, não só um check interno do container. `external-ip` não foi necessário (a VPS tem IP público direto na interface, sem NAT).
12. **Onboarding simplificado a pedido do usuário, `v1.0.2`**: três mudanças no fluxo de conta, todas verificadas ao vivo contra produção via o app empacotado de verdade (CDP no processo instalado), não só teste automatizado.
    - Cadastro já autenticava automaticamente — nenhuma mudança de código, só confirmado (o texto da tela de cadastro já dizia isso desde a fatia 5/6 da FASE 11).
    - Login passou a aceitar só usuário + senha. Decisão explícita do usuário: **manter** o comportamento de username poder se repetir (não forçar único) — `Accounts.authenticate_user/2` novo resolve por conta própria quando só existe uma conta com aquele nome; quando há mais de uma, responde `:ambiguous_username` (422) e o cliente (`LoginPage.tsx`) revela um campo de código só nesse caso, sem nunca tentar a senha contra várias contas pra adivinhar qual é.
    - Cadastro entra automaticamente no servidor único — `DEFAULT_SERVER_ID` (env var opcional, configurada em produção com o id do servidor "TORA DOS BURRO" real) + `Servers.join_server/2`, que já existia desde a FASE 3. Botão de entrar por convite deixado no cliente (não atrapalha, pode ser útil se um dia existir um segundo servidor).
    - Verificação ao vivo real (não só `mix test`): registro via `curl` contra `http://2.28.229.48` confirmando token imediato; login só com usuário via `curl`; e o instalador `v1.0.2` de verdade instalado e dirigido via CDP (`--remote-debugging-port`) confirmando na UI real — ícone do servidor aparecendo sem convite, e o campo de código de fato aparecendo só quando duas contas de teste tinham o mesmo nome.
13. **Bug real achado pelo usuário na v1.0.2, corrigido na `v1.0.3`: voz/chat em tempo real nunca conectava de verdade contra produção pelo app empacotado** — ficava preso em "Conectando…", WebSocket sempre rejeitado com 403. Causa: `check_origin` do Phoenix (default `true`) nunca foi desligado em produção — só existe em `dev.exs`, e isso é boilerplate genérico do `mix phx.new`, sem relação nenhuma com o Electron; ninguém tinha de fato testado voz do app empacotado contra a VPS real antes (toda verificação anterior de voz/chat do cliente rodou contra `localhost:4000`). O `Origin` que o Electron empacotado manda (carregando de `file://`) nunca bate com o host configurado — um cliente de WebSocket comum sem header `Origin` nenhum passa direto, por isso nenhum teste de protocolo cru pegou isso antes. Corrigido com `check_origin: false` só no transporte `/socket` (não no `/live`) em `endpoint.ex` — `UserSocket.connect/3` já autentica só por token Guardian no query param, nunca cookie de navegador, então a proteção que `check_origin` oferece (contra site malicioso usando sessão de navegador) nem se aplica a esse cliente.
    - **Achado investigando o mesmo relato, endurecimento relacionado**: o cliente nunca renovava o access token (dura 15min) durante a sessão, só no boot — uma sessão de voz/chat aberta por muito tempo, se o socket precisasse reconectar por qualquer motivo (rede, um deploy no servidor), ficava presa reconectando pra sempre com o token velho. `authStore.ts` agora renova a cada 10min; `socket.ts` usa uma função pra `params` (token sempre atual) em vez de um valor fixo desde a conexão original — sem derrubar uma conexão saudável só pra atualizar o token.
    - Verificado contra o bug real do usuário: extraí o token da sessão travada dele (`window.api.secureStorage`), confirmei que `/users/me` aceitava mas o handshake de WebSocket cru rejeitava com 403 — descartando a hipótese inicial (token expirado) e apontando pro `check_origin`. Depois do fix, testei de novo do zero (conta nova, app empacotado `v1.0.3` de verdade via CDP): painel de voz conecta e mostra os controles reais (mutar/ensurdecer/câmera/tela). O único erro que sobra é do Go Live tentando conectar num LiveKit que não existe (`livekit.placeholder.invalid`) — limitação já conhecida e documentada, sem relação com esse bug.
14. **Identidade visual própria, `v1.0.4`** — pedido do usuário ("interface bonita, gamer"), deliberadamente fugindo do roxo-neon-em-preto que qualquer gerador cairia por padrão. Sistema definido em `desktop/src/renderer/src/index.css` (tokens via `@theme` do Tailwind v4): fundo quase-preto com leve tom azul-violeta, dupla de acento com significado (volt = verde-limão elétrico, estado positivo/ativo; plasma = magenta, estado quente/ao vivo/perigo — não um acento neon genérico só), tipografia técnica (Chakra Petch/Manrope/JetBrains Mono), corte chanfrado no canto superior direito como assinatura estrutural repetida em superfícies grandes. Elemento de assinatura: no painel de voz, quem está com o microfone aberto ganha um contorno com glow pulsante ("microfone quente") — ligado ao estado real da chamada, não só decoração. Nenhuma mudança de funcionalidade, só `index.css` + todos os componentes/páginas do `desktop/`.
    - **Achado no build de produção (não em dev)**: `@import url(...)` pro Google Fonts dentro do CSS violava a regra de que `@import` precisa ser a primeira regra — o Lightning CSS do build de produção sinalizava isso (dev não, o Vite trata CSS diferente em dev). Corrigido trocando por um `<link rel="stylesheet">` em `index.html` — mais robusto de qualquer forma, não depende de ordem de regras CSS. CSP (`connect-src`/`style-src`/`font-src`) estendida pra `fonts.googleapis.com`/`fonts.gstatic.com`, mesma classe de gotcha já documentada nesse projeto pro LiveKit.
    - Verificado ao vivo: dev server real com estado autenticado forçado via import dinâmico dos stores (técnica já estabelecida no projeto) pra ver todas as telas sem precisar de backend, e depois o instalador `v1.0.4` de verdade — instalado, rodando, capturado via `Page.captureScreenshot` do protocolo CDP (não só `Runtime.evaluate` como nas verificações anteriores).
15. **Moldura da janela personalizada, `v1.0.5`/`v1.0.6`** — dois pedidos seguidos do usuário. `v1.0.5`: `Menu.setApplicationMenu(null)` tira a barra File/Edit/View/Window que o Electron gera sozinho sem isso (nenhuma das ações faz sentido pra esse app). `v1.0.6`: janela sem moldura nativa nenhuma ("borda infinita") — `frame: false` na `BrowserWindow` + `TitleBar.tsx` novo desenhando barra de título/minimizar/maximizar-restaurar/fechar do zero, no mesmo sistema visual (bevel, volt, tipografia técnica), agindo via IPC novo (`window.api.windowControls.*`) porque o renderer nunca pode mexer na própria janela sozinho.
    - **Verificação exigiu um método novo**: menu e moldura de janela são chrome nativo do SO, não fazem parte do DOM da página — nem o Browser pane nem `Page.captureScreenshot` via CDP mostram isso (só capturam a área de conteúdo). Usado `GetWindowRect`/`CopyFromScreen` via PowerShell pra tirar um print da janela real inteira, e cliques de verdade nos 3 botões custom confirmando efeito real (`IsIconic` pra minimizar, tamanho da janela mudando pra maximizar, porta do CDP parando de responder — processo encerrado de verdade — pro fechar), não só que o handler disparou.
16. **Voz e compartilhamento de tela no estilo Discord, `v1.0.7`** — quatro pedidos do usuário juntos:
    - Clicar num canal de voz já entra direto (sem botão "Conectar" separado) — `HomePage.tsx` decide isso no `onSelect`, comparando com o canal de voz atualmente conectado pra não reentrar à toa.
    - Lista de participantes (ícone + nome) aparece embaixo do canal de voz na barra lateral — recorte deliberado: só do canal em que a pessoa **realmente está conectada**, não uma prévia de canais não-joinados (isso pediria um mecanismo novo de presença no backend, fora de escopo desse pedido).
    - **Indicador de fala de verdade** (`webrtc/SpeakingDetector.ts`, novo) — um `AnalyserNode` por stream (mic local + cada peer remoto). Primeira tentativa usava a média de amplitude no domínio da **frequência** (`getByteFrequencyData`) e falhava: testado ao vivo com um tom sintético puro (oscillator), a energia toda ficava concentrada em 2-3 bins de frequência, e a média com os outros ~250 bins (quase todos zero) afogava completamente o sinal — mesmo com o tom bem alto, a média nunca cruzava o limiar. Corrigido usando RMS no domínio do **tempo** (`getByteTimeDomainData`), que mede volume de verdade (o quanto a onda se afasta do centro) independente de quantas frequências o som ocupa. Confirmado depois do fix: RMS ≈ 81 com o tom alto, RMS = 0 em silêncio.
    - Qualidade do compartilhamento de tela (720p/1080p/1440p, 30/60 FPS) — `ScreenSharePicker.tsx` ganha os seletores, aplicados como constraints `ideal` (não `exact`, pra não falhar o compartilhamento inteiro numa tela física menor que o pedido) no `getDisplayMedia`.
    - Grade com destaque quando 2+ pessoas compartilham tela ao mesmo tempo — `ScreenShareGrid` novo em `VoicePanel.tsx`: uma tela grande em foco + tira de miniaturas clicáveis: a pessoa escolhe quem fica em evidência. Só aparece com mais de 1 compartilhamento simultâneo (uma tela só continua como antes, sem grade).
    - **Verificado ao vivo com duas contas reais simultâneas** (app empacotado de verdade, `--use-fake-device-for-media-stream` + `--user-data-dir` separado por instância, técnica nova nesse projeto): entrar direto confirmado nos dois lados; roster confirmado nos dois lados; indicador de fala confirmado via tom sintético (o áudio falso padrão do Chromium sozinho não era alto o suficiente pra cruzar o limiar — substituí `getUserMedia` por um oscillator alto via `Runtime.evaluate` pra validar o pipeline de verdade, sem precisar de microfone físico); qualidade confirmada lendo `getSettings()` da track capturada de verdade (`2560x1440 @ 60fps`, exatamente o pedido); troca de foco na grade confirmada com dois compartilhamentos reais simultâneos, um de cada instância.
17. **Trocar a qualidade da tela já transmitindo + tela compartilhada responsiva, `v1.0.8`** — dois pedidos do usuário juntos:
    - `voiceStore.ts` ganha `updateScreenShareQuality(quality)`, acionada por um novo `ScreenShareQualityMenu` (popover ao lado de "PARAR TELA" em `VoicePanel.tsx`, reaproveitando os mesmos `RESOLUTIONS`/`FRAME_RATES` do `ScreenSharePicker.tsx`, agora exportados). Recaptura a mesma fonte (`desktopCapturer` source id, guardado em `lastScreenSourceId`) nos novos parâmetros e entrega a track nova pro `mesh.setScreenTrack()` já existente — mesmo caminho de `replaceTrack` que o início do compartilhamento e a troca de câmera já usam, então não renegocia nem manda evento novo pro servidor.
    - **Bug real achado e corrigido no processo**: a primeira versão pedia a nova captura (`getDisplayMedia`) *antes* de parar a antiga. Do lado de quem compartilha, tudo parecia certo (track nova, sem erro, `connectionState: connected`) — mas `getStats()` no `RTCRtpSender` (`outbound-rtp`) provou que o encoder continuava mandando a resolução **antiga**, com `qualityLimitationReason: "none"` (descartando adaptação por banda/CPU como causa): o Chromium, com a captura anterior daquela mesma fonte ainda viva no momento do pedido, reaproveita a sessão já em andamento e ignora os parâmetros novos por completo. Corrigido invertendo a ordem — para a track atual antes de pedir a próxima; se a recaptura falhar depois disso, não dá mais pra manter o compartilhamento anterior rodando, então o código chama o mesmo `stopScreenShare()` de sempre (limpeza completa, incluindo avisar o servidor) em vez de deixar um estado "compartilhando" com uma track morta.
    - De quebra, o vídeo da tela compartilhada (single view e o destaque da grade) ganhou um prop `large` no `RemoteVideo` de `VoicePanel.tsx` — troca o teto de `max-w-2xl` (672px, mesmo limite usado pra câmera/Go Live) por preencher a largura do painel. Escopo deliberado: só a tela compartilhada, não os outros tipos de vídeo, seguindo a reclamação específica do usuário.
    - **Verificado ao vivo com duas contas reais na mesma chamada de voz**: troquei a qualidade várias vezes em sequência (720p30 → 1440p60 → 1080p30, depois de volta pra 720p30) e, a cada troca, conferi não só o que a track local dizia ter (`getSettings()`), mas o tamanho real do frame **decodificado** do lado de quem recebe (`videoWidth`/`videoHeight` do elemento de vídeo) — confirmando que o vídeo muda de verdade pro outro lado, nos dois sentidos, sem cair a chamada. Também testado clicando de verdade no popover novo (não só chamando a store direto). Achado incidental do processo de teste (não é bug do app): reimportar os módulos da store por `import()` dinâmico depois de uma edição em dev, sem recarregar a página, pode pegar uma cópia duplicada do módulo (efeito colateral do versionamento de URL do HMR do Vite) — contornado testando sempre a partir de uma instância recém-aberta depois de qualquer edição de código, não só um `location.reload()`.
18. **Bug real reportado pelo usuário logo após a v1.0.8, corrigido na `v1.0.9`**: o `ScreenShareQualityMenu` novo fechava sozinho enquanto a pessoa arrastava o mouse pra cima em direção à opção desejada — sem tempo de clicar. Causa: fechava no `onMouseLeave` do `<div className="relative">` que envolve botão + popover, mas o popover é `position: absolute` — isso o tira do fluxo normal, então a caixa desse `div` não se estende até cobrir o vão visual entre o botão e o popover (só o botão, seu único filho em fluxo normal, contribui pro tamanho do contêiner); ao cruzar esse vão pra alcançar uma opção, o cursor passava por cima de algo que não é o contêiner nem o popover, disparando `mouseleave` cedo demais. Corrigido trocando o fechamento por clique-fora (`mousedown` no `document` + `ref`, fecha se o alvo não está dentro do contêiner) em vez de depender de hover — elimina essa classe inteira de bug e é o padrão mais comum pra esse tipo de menu de qualquer forma.
    - **Verificação exigiu simular o gesto exato relatado, não só um clique**: `element.click()`/`dispatchEvent()` via JS não reproduzem o bug (o navegador só calcula `mouseenter`/`mouseleave` "de verdade" a partir de movimento real do ponteiro, não de eventos sintéticos disparados em script) — usado `Input.dispatchMouseEvent` do protocolo CDP (mesmo mecanismo por trás de ferramentas de automação de UI) pra mover o mouse em vários passos pequenos do botão até uma opção, atravessando exatamente o vão do bug, e só então clicar. Confirmado que o clique registrava (a qualidade realmente mudava) e que clicar fora ainda fechava o menu normalmente.
19. **Aba de Configurações, `v1.1.0`** — pedido do usuário: "configurações básicas", detalhado na hora pro microfone — cancelamento de eco, supressão de ruído, sensibilidade ajustável, salvos por usuário.
    - `stores/settingsStore.ts` novo — `MicSettings` (`echoCancellation`/`noiseSuppression`/`micSensitivity`), persistido em `localStorage` (não `window.api.secureStorage`) sob a chave `tora-mic-settings:{userId}`. Decisão deliberada: configuração de microfone é sobre o ambiente físico da pessoa (sala, hardware), não algo que devesse sincronizar entre aparelhos diferentes via conta no backend — por isso local, não servidor; "por usuário" significa por conta logada *nesse* aparelho. `HomePage.tsx` carrega a configuração da conta assim que sabe quem é (`loadForUser`), bem antes de qualquer chance de entrar numa chamada.
    - `components/SettingsModal.tsx` novo — aberto por um ⚙ no cabeçalho, mesmo padrão visual de `ScreenSharePicker.tsx`/`JoinServerDialog.tsx`. Dois toggles (switch construído só com utilitários do Tailwind, sem componente novo) + um slider de sensibilidade com **medidor de volume ao vivo** (mesma técnica RMS no domínio do tempo do `SpeakingDetector.ts`, reimplementada localmente porque a API dele é por vários peers com hold-time, não serve pra uma prévia única) com uma marca mostrando o limiar atual — sem isso, ajustar "sensibilidade" seria escolher um número às cegas.
    - `SpeakingDetector.ts` ganha `setThreshold()` e exporta o antigo `SPEAKING_THRESHOLD` (agora só o valor padrão, usado também como default do settingsStore). `voiceStore.ts` lê `useSettingsStore.getState().mic` uma vez, em `join()`, pros constraints do `getUserMedia` (eco/ruído) e o limiar inicial do detector (sensibilidade); ganha `setMicSensitivity(value)` nova, um wrapper fino em volta do `speakingDetector` (privado ao módulo) pra ajuste ao vivo em chamada — única forma de alcançar essa instância de fora. Eco/ruído ao vivo não passam pelo voiceStore: como são `MediaTrackConstraints`, o próprio `SettingsModal.tsx` chama `applyConstraints()` direto na track (a da chamada real, se já conectado; senão a de uma captura de prévia própria, aberta só pro medidor e fechada ao sair da tela) — dependência de import é só numa direção (`voiceStore` → `settingsStore`), sem ciclo.
    - **Verificado ao vivo**: toggles aplicando de verdade — confirmado interceptando `MediaStreamTrack.prototype.applyConstraints` e vendo a chamada real acontecer, tanto numa track de prévia quanto na track real de uma chamada ativa. Sensibilidade verificada nos dois sentidos com um tom sintético de volume conhecido (RMS medido) alimentando um `getUserMedia` trocado por monkeypatch: limiar baixo → detectado como "falando"; limiar alto → não detectado, exatamente o esperado, tanto via `voiceStore.setMicSensitivity()` direto quanto arrastando o slider de verdade na UI. Persistência confirmada lendo o `localStorage` de volta, não só o estado em memória. Achado no processo (armadilha do próprio teste, não bug do app): o primeiro tom sintético usava um único `MediaStreamAudioDestinationNode`/track reaproveitado entre "capturas" — assim que uma tela de configurações fechava (parando a track ao desmontar, comportamento correto), qualquer captura "nova" que reusasse o mesmo objeto já vinha morta; corrigido criando um destino novo por captura, a partir do mesmo oscillator.
20. **Quem está online (barra da direita) + bandeja do Windows, `v1.2.0`** — dois pedidos do usuário juntos.
    - `channels/server_channel.ex` novo (topic `server:{server_id}`) — presença de "quem está online no app inteiro", não confundir com a presença já existente por sala de voz (`voice:{id}`). Só join (checando membership via `Servers.fetch_server/1` + `Servers.fetch_member/2`, mesmo padrão dos outros canais) + `Presence.track`, nenhum evento client→server. Registrado em `user_socket.ex` (`channel "server:*", ...`). 4 testes novos (`server_channel_test.exs`, mesmo molde de `voice_channel_test.exs`): membro entra e vê `presence_state`, quem entra depois vê quem já tava, não-membro é rejeitado, servidor inexistente também é rejeitado (não vaza `not_found` — sempre `forbidden`), sair remove da presença. 99 testes no total agora.
    - `stores/presenceStore.ts` novo no cliente — entra em `server:{id}` sempre que `selectedServerId` muda (efeito em `HomePage.tsx`), sai no logout. `components/MemberList.tsx` novo — coluna da direita, mesmo estilo do Discord (ONLINE/OFFLINE, pontinho verde/cinza no avatar), cruzando `onlineUserIds` com os `members` que `serversStore.ts` já busca. Herda a mesma limitação já documentada pro roster de voz (`ChannelList.tsx`): `members` só é buscado uma vez por seleção de servidor, então alguém que entra no servidor depois não aparece (nem online nem offline) até reselecionar — decisão consciente de não resolver agora, mesma classe de limite aceito antes.
    - `main/index.ts` ganha `createTray()` — fechar a janela (✕ do TitleBar, `Alt+F4`, qualquer coisa que dispare o `close` da janela) não encerra mais o processo, só esconde (`event.preventDefault()` + `win.hide()`), a não ser que uma flag `isQuitting` esteja marcada — só acontece no "Sair" do menu da bandeja e num `app.on('before-quit', ...)` novo (cobre qualquer outro caminho real de saída, não só o item do menu). Motivo do pedido: Chromium não suspende WebRTC/`getUserMedia` de uma janela escondida (só de uma fechada de verdade), então uma chamada de voz continua rodando com a janela na bandeja.
    - **Achado no processo (empacotamento, não dev)**: `build/icon.ico` — já usado pelo electron-builder pro ícone do `.exe`/instalador — não entra no pacote final por padrão, só alimenta esses dois usos específicos; um `nativeImage.createFromPath()` apontando pra lá dentro do app empacotado resolveria pra um caminho que não existe em disco. Corrigido com `extraResources` novo em `electron-builder.yml` (copia pra `resources/icon.ico`) e `trayIconPath()` ramificando em `app.isPackaged`.
    - **Verificado ao vivo**: quem-está-online com duas contas reais na mesma sessão (uma vendo a outra aparecer/sumir da lista ao entrar/sair). Bandeja via um bridge de IPC temporário só pra teste (`debug:show-window`/`debug:quit`, removido depois de confirmar) — clique real em ícone/menu de bandeja nativo não é algo que dê pra automatizar via CDP: fechar de fato só esconde (processo/webContents continuam respondendo, confirmado tanto em dev quanto no instalador `v1.2.0` de verdade), restaurar reexibe e foca a janela, "Sair" encerra o processo de verdade.
21. **Entrar/sair de uma transmissão específica + som do PC no Go Live, `v1.3.0`** — dois pedidos do usuário juntos.
    - **Assistir vira escolha, não automático**: `goLiveStore.ts` conecta com `autoSubscribe: false` (precisou repassar isso também na reconexão de dentro de `startGoLive`, já que reconectar volta pro padrão `true` do LiveKit se não for repassado de novo). `VoicePanel.tsx` ganha `GoLiveStreams` novo — quem está ao vivo aparece como cartão "● fulano está ao vivo" com **ENTRAR**; só esse clique chama `watchStream(peerId)`, que dá `setSubscribed(true)` em cada `RemoteTrackPublication` dessa pessoa (lidas direto de `room.remoteParticipants`, sem cache paralelo — o próprio `Room` do LiveKit já mantém isso). **SAIR DA TRANSMISSÃO** desinscreve de verdade e já limpa o stream local na hora, sem esperar o `TrackUnsubscribed` assíncrono. `watchingUserIds` sobrevive a reconexões do publicador (reinscrição automática via `RoomEvent.TrackPublished`, checando se quem publicou está em `watchingUserIds`) e é podado quando a presença mostra `live: false` pra alguém, pra uma próxima transmissão da mesma pessoa ser uma inscrição limpa, não uma intenção velha apontando pra conexão morta.
    - Vídeo e áudio de um mesmo participante agora se juntam num MediaStream só (por `participant.identity`) — `TrackSubscribed`/`TrackUnsubscribed` passaram a tratar os dois `kind`s (antes só vídeo; Go Live não tinha áudio nenhum até aqui, a track chegava e era descartada). Um `<video>` só toca as duas juntas.
    - **Som do PC**: `startGoLive(sourceId, includeSystemAudio)` novo — quando marcado, `getDisplayMedia({video:true, audio:true})`, com `main/index.ts` resolvendo `audio: 'loopback'` (única forma documentada do Electron nesse fluxo de picker customizado; WASAPI, só Windows, sistema inteiro — não dá pra isolar só a fonte de vídeo escolhida). **Achado real testando, não hipotético**: numa placa de áudio USB sem fio de verdade (a máquina de teste desta sessão), essa captura falha com `NotReadableError: Could not start audio source` — reproduzido igual com `'loopbackWithMute'`, e bate com uma sequência de issues abertos no repositório do próprio Electron ao longo de anos (`#17045`, `#15829`, `#41697`, `#49607`) sempre ligados a dispositivos de áudio específicos no Windows; não é algo corrigível pelo código desse app (um pacote de terceiros que existe pra contornar isso documenta explicitamente que Electron 39+ — esse app está no 44 — *não precisa* dele, confirmando que é uma lacuna de driver, não de funcionalidade faltando). Por ser uma falha real e reproduzível, `startGoLive` trata como esperado: se a captura com áudio falhar, tenta de novo só com vídeo (chamando `selectSource` de novo com `includeAudio: false`, já que o estado pendente em `main/index.ts` é de uso único) em vez de travar a transmissão inteira por causa do áudio, e avisa com um erro não-fatal em vez de bloquear.
    - **Mute anti-retorno**: `voiceStore.ts` ganha `localPlaybackMuted` (diferente de mute/deafen, que mexem no microfone, não na reprodução local) — enquanto o som do PC está sendo capturado e publicado de verdade, muda pra mudo toda reprodução de áudio que o próprio app está tocando (parceiros de voz em `RemoteAudio`, qualquer outra transmissão sendo assistida), porque a captura de loopback pega TUDO que o Windows está tocando, inclusive a própria chamada — sem isso, quem estivesse assistindo E na mesma chamada ouviria a conversa duas vezes. Ligado só depois de uma publicação com áudio bem-sucedida (nunca no fallback só-vídeo), desligado sempre em `stopGoLive`/`leave()` como proteção. Dependência de import numa direção só (`goLiveStore` → `voiceStore`), sem ciclo.
    - **Verificado ao vivo o que dava pra verificar sem LiveKit rodando de verdade** (produção continua em `livekit.placeholder.invalid`, confirmado de novo agora via um join real contra produção com uma conta descartável; sem Docker aqui pra subir um LiveKit local — mesma limitação de sempre, documentada desde a FASE 9): captura de áudio do sistema de verdade (tom real tocado nos alto-falantes do computador, capturado e medido via `AnalyserNode`, RMS não-zero enquanto o tom tocava); o fallback pra só-vídeo quando a captura com áudio falha (confirmado terminando com vídeo real, `hasAudio: false`); o mute anti-retorno com uma chamada de voz real entre duas contas (`<audio>` do outro lado mutando/desmutando de verdade em resposta a `setLocalPlaybackMuted`). O checkbox de som no seletor de fonte e o cartão entrar/sair foram confirmados pela renderização real (forçando o estado local do componente pra abrir o seletor/cartão direto, mesmo padrão já usado e comprovado pro seletor de qualidade) — o botão "Ir ao vivo" de verdade fica bloqueado sem uma conexão LiveKit bem-sucedida em qualquer ambiente disponível agora, então o clique real nele não dá pra testar até existir um LiveKit de verdade rodando em algum lugar.
22. **Som do PC no compartilhamento de tela normal (mesh) + volume por botão direito, `v1.4.0`** — dois pedidos do usuário juntos.
    - **Tela com/sem som**: o checkbox "Incluir som do PC" do `ScreenSharePicker.tsx`, que só existia pro Go Live (`v1.3.0`), passou a valer também pro compartilhamento de tela normal — `startScreenShare(sourceId, quality, includeAudio)` ganha um parâmetro novo, mesmo mecanismo de captura/fallback do Go Live (`audio: 'loopback'`, retry só-vídeo se falhar). A diferença real: aqui a track de áudio precisa entrar no **mesh** (N conexões par-a-par), não numa SFU única — `MeshManager` ganha um terceiro sender preguiçoso por peer (`screenAudioSender`), e o Presence ganha `screen_sharing_audio` pra quem recebe distinguir a track de áudio nova (mic sempre existe; som da tela só às vezes) sem inspecionar a track em si. `setScreenTrack`/`setScreenAudioTrack` recebem o mesmo `MediaStream` local de propósito — o WebRTC agrupa as duas tracks pelo mesmo `msid`, então um `<video>` só já toca vídeo+áudio da tela juntos, mesma ideia já usada no Go Live pra juntar tracks por `participant.identity`.
    - `localPlaybackMuted` (mute anti-eco, `v1.3.0`) vira um contador de origens (`Set<'screenshare' | 'golive'>`) em vez de bool solto — `addSystemAudioSource`/`removeSystemAudioSource` substituem o antigo `setLocalPlaybackMuted`, já que agora dá pra estar transmitindo Go Live E compartilhando tela com som ao mesmo tempo, e só faz sentido desmutar quando a última das duas parar.
    - `updateScreenShareQuality` (trocar resolução/fps já compartilhando, `v1.0.8`) passou a preservar `includeAudio` na recaptura — antes, trocar a qualidade enquanto compartilhava com som derrubava o áudio sem avisar.
    - **Bug achado na revisão, corrigido antes de qualquer teste ao vivo**: a primeira versão da desambiguação de áudio (mic vs. som da tela) usava `!state.remoteAudioStreams[peerId]` (mic ainda não existe) como parte do fallback — errado pro caso mais comum, mic quase sempre já existe (conecta no início da chamada) bem antes de alguém ligar som da tela (ação manual, minutos depois); esse fallback nunca dispararia no caso real. Corrigido comparando a **referência** do stream (`state.remoteAudioStreams[peerId] !== stream`) em vez de só existência.
    - **Volume por botão direito**: `VolumeMenu` novo em `VoicePanel.tsx` — popover com slider 0–200% (estilo Discord), abre no ponto do clique, fecha só ao clicar fora (mesmo padrão já comprovado do `ScreenShareQualityMenu`, `v1.0.9`). Guardado por pessoa em `voiceStore.ts` (`remoteMicVolumes`/`remoteScreenVolumes`) e `goLiveStore.ts` (`remoteVolumes`) — nunca na própria tile (sem sentido ajustar volume do que você mesmo está mandando). `volume` de um `<audio>`/`<video>` é propriedade do elemento, não atributo — não dá pra controlar de forma confiável só com prop JSX, aplicado via `ref`+`useEffect` (mesma técnica já usada pro `srcObject`). Achado incidental corrigido no caminho: as miniaturas da grade de telas simultâneas (`ScreenShareGrid`) tocavam áudio de todo mundo ao mesmo tempo que a tela em destaque — inofensivo antes (tela nunca tinha áudio), viraria cacofonia real com 2+ pessoas compartilhando com som; miniaturas agora são sempre mudas, só a tela em destaque toca.
    - **Verificado ao vivo o que dava pra verificar**: o checkbox "Incluir som do PC" aparecendo de verdade também no compartilhamento de tela normal (antes só Go Live); a mesma falha real de hardware do Go Live (`NotReadableError`, mesma máquina de teste) reproduzindo igual aqui, confirmando que o fallback pra só-vídeo funciona nesse caminho novo também (tela real 1080p30 compartilhada com sucesso depois do fallback). O menu de volume (botão direito, slider, fechar ao clicar fora) foi verificado só contra a própria entrada da pessoa na lista de participantes (guarda de "não mexer no próprio volume" confirmada — botão direito em si mesmo não abre nada, sem erros no console) — não deu pra verificar a interação completa (abrir menu numa pessoa remota, arrastar o slider, ver o `<audio>`/`<video>` real mudar de volume) porque não havia um segundo peer real disponível nesta sessão (tentativas de rodar uma segunda instância do Electron com sessão isolada não deram certo no ambiente Windows/Git Bash desta sessão) e injetar um peer falso via store, pra testar sem um segundo peer, esbarrou em instabilidade do próprio ambiente de teste (não do código) depois de muitas rodadas. O mecanismo em si (popover + `ref`+`useEffect` pra volume) reaproveita exatamente os padrões já comprovados do `ScreenShareQualityMenu` e do medidor de volume do `SettingsModal.tsx` — risco considerado baixo, mas fica registrado como não verificado de ponta a ponta.
23. **Sons de identificação, `v1.5.0`** — pedido do usuário: bips curtos ao entrar/sair da call, mutar/desmutar, e quando alguém começa ou para uma transmissão.
    - `services/soundCues.ts` novo — sintetiza os bips na hora via Web Audio API (osciladores + envelope de ganho ataque/decaimento), sem nenhum arquivo de áudio novo no repositório; reaproveita um único `AudioContext` do módulo, igual ao padrão já usado no `SpeakingDetector`/medidor de volume do `SettingsModal`. Seis sons: entrar/sair da call (duas notas, ascendente/descendente), mutar/desmutar (uma nota curta cada) e alguém ficar/parar de estar ao vivo (três notas, mais chamativo por ser um evento de outra pessoa). Falha silenciosa de propósito (`try/catch` vazio) — um som de identificação nunca deveria derrubar a ação real por causa de alguma restrição de áudio do navegador/SO.
    - `voiceStore.ts`: som de entrar toca no fim de um `join()` bem-sucedido; som de sair só toca se havia algo real pra sair (`wasActive = status !== 'idle'`, checado ANTES do reset) — sem essa guarda, o som de "saiu" tocaria toda vez que a pessoa entrasse pela primeira vez, já que `join()` sempre chama `leave()` primeiro como reset defensivo. Mutar/desmutar toca no fim de `toggleMute()`; `toggleDeafen()` deliberadamente não ganhou som (não foi pedido, e são ações conceitualmente diferentes mesmo compartilhando o campo `localMuted` por baixo).
    - `goLiveStore.ts`: `presence.onSync` agora compara quem estava "ao vivo" antes contra a lista nova (lida via `get()`, antes do `set()` substituir `participants`) pra saber quem começou/parou de transmitir desde o último sync — toca o som correspondente pra cada transição, incluindo a própria (funciona como confirmação de "sua transmissão começou", igual ao som de entrar na call).
    - `settingsStore.ts` ganha `soundCuesEnabled` (default ligado), persistido na mesma chave `localStorage` já usada pro microfone (`tora-mic-settings:{userId}` — o nome ficou por herança, já guarda mais que só microfone; não valeu a pena migrar de chave só por isso) — toggle novo "Sons de identificação" no topo do `SettingsModal.tsx`.
    - **Verificado ao vivo, contando osciladores de verdade em vez de tentar "ouvir" via CDP**: interceptado `AudioContext.prototype.createOscillator` na página real (Electron via CDP) pra contar quantas notas cada ação dispara. Sequência limpa (conectar → mutar → desmutar) confirmou exatamente 2 + 1 + 1 = 4 osciladores, batendo com o desenho de cada som nota por nota. O toggle de ligar/desligar confirmado nos dois sentidos (desligado: nenhum oscilador novo mesmo entrando na call de verdade; religado: volta a soar) — e, de quebra, confirmou que a preferência persiste corretamente entre sessões (um teste anterior que desligou o som via essa mesma action deixou `soundCuesEnabled: false` salvo no `localStorage`, e uma sessão nova carregada depois respeitou isso sem precisar de nada explícito). Som de "alguém ao vivo" (start/stop) não foi possível verificar contra um segundo peer real nesta sessão (mesma limitação já registrada no item 22) — mas reaproveita a mesma função `playSequence` já comprovada pros outros cinco sons, só com notas diferentes, então o risco de comportamento diferente é baixo.
24. **Quatro bugs reais reportados pelo usuário depois de testar com gente de verdade, `v1.6.0`**.
    - **"Desconhecido" no chat/voz, e a barra de quem-está-online incompleta (as duas eram o mesmo bug)**: `serversStore.members` (cliente) sempre foi buscado **uma vez só**, na seleção do servidor (já documentado como limitação aceita desde a v1.2.0) — quem entrava depois nunca aparecia, nem no roster de voz (virava "desconhecido") nem na barra da direita (nem online nem offline, já que `MemberList.tsx` só cruza `onlineUserIds` contra esse `members` parado). Corrigido no backend: `Servers.notify_member_joined/3`, novo, faz `broadcast` de `member:joined` no topic `server:{id}` (o mesmo que `ServerChannel`/`presenceStore.ts` já usam pra "quem está online") — coberto pelos dois caminhos que fazem alguém entrar num servidor, registro com auto-join (`AuthController.register/2`) e convite (`Servers.use_invite/2`). **Ponto de atenção real, não hipotético**: `use_invite/2` chama `join_server/2` de **dentro** de uma `Repo.transaction` — um broadcast não é transacional, dispara na hora, então NÃO podia morar dentro de `join_server`/`do_add_member` (chamado nos dois caminhos), senão um rollback da transação (ex.: usuário banido, checado só depois do `join_server` já ter "sucedido" dentro dela) deixaria quem estava olhando acreditando que alguém entrou que, no banco, nunca entrou de verdade. `notify_member_joined/3` só é chamado pelos dois chamadores DEPOIS de confirmação real (commit da transação em `use_invite/2`; insert direto, sem transação, em `register/2`). Cliente: `presenceStore.ts` (que já tinha o canal `server:{id}` aberto) ganha um `channel.on('member:joined', ...)` novo, chamando `serversStore.addMember(serverId, member)` novo (dedupe por `user.id`, ignora se for pra um servidor diferente do selecionado agora).
    - **Testes novos** (`server_channel_test.exs`, mesmo molde dos já existentes): quem já está no servidor recebe o `member:joined` ao vivo quando alguém entra por convite; um convite que dá rollback de verdade (usuário banido, forçado a falhar **dentro** da transação, não só o `cond`-check de topo de `use_invite/2` que nem chega a abrir transação) **não** dispara o broadcast — prova direta de que o timing (depois do commit, nunca de dentro dele) está certo. 102 testes no total agora.
    - **Verificado ao vivo com contas reais, sem precisar de uma segunda instância do Electron**: instância única logada como dono, servidor selecionado (`MemberList` mostrando só "Você"); por fora, via `curl` direto no backend, uma segunda conta registrada e entrando pelo convite — o cliente já aberto mostrou o novo membro na hora, sem reselecionar o servidor nem recarregar a página.
    - **Quem transmite com som do PC não conseguia ouvir a própria call**: `localPlaybackMuted` (anti-eco, v1.3.0/v1.4.0) mutava TAMBÉM o `RemoteAudio` dos parceiros de voz — sem querer: a call é a única coisa que quem está transmitindo realmente precisa continuar ouvindo. Corrigido tirando `localPlaybackMuted` dessa mute condition (`VoicePanel.tsx`, fica só `localDeafened`) — a própria tela/Go Live sendo assistidos continuam mudos nesse cenário (esse motivo de mute continua válido: evitar recapturar pelo loopback um áudio que a própria pessoa está tocando). **Tradeoff aceito conscientemente, documentado**: a voz de quem transmite agora entra na captura de loopback igual a qualquer som do PC — quem estiver assistindo E na mesma call pode ouvir um leve eco com atraso da própria fala do streamer; mitigável de verdade com o volume por pessoa/transmissão já existente (botão direito), não algo que precisasse de uma solução automática nova.
    - **App inteiro travava (tela em branco) ao mexer no volume de outra pessoa** — bug real e sério, confirmado por um usuário de verdade. Causa: o slider de volume (v1.4.0) ia até 200% ("boost", estilo Discord), mas `HTMLMediaElement.volume` só aceita `[0, 1]` — atribuir um valor fora disso lança `DOMException`, e sem nenhum error boundary no app (confirmado faltando numa investigação anterior desta mesma sessão) uma exceção não capturada dentro do `useEffect` que aplica o volume derrubava a árvore inteira do React. Ironicamente, essa é a explicação real de um "mistério" da sessão da v1.4.0: um crash não-explicado durante os próprios testes, na hora atribuído a "instabilidade do ambiente de teste" — não era o ambiente, era esse mesmo bug, disparado ao arrastar o slider de teste até 150%. Corrigido capando o slider em 100% (`VolumeMenu`, `VoicePanel.tsx`) e, como segunda camada, um `clampVolume()` novo aplicado direto no `.volume =` de `RemoteAudio`/`RemoteVideo` — mesmo que algum caminho futuro reintroduza um valor fora de `[0,1]`, não derruba o app de novo. Fazer o boost de verdade (>100%) precisaria de um `GainNode` via Web Audio API por elemento — escopo bem maior que o pedido original, não construído.
    - **Verificado ao vivo, causa raiz confirmada direto, não só inferida**: interceptado `AudioContext`/erros globais na página real; chamar `setRemoteMicVolume(peerId, 1.5)` direto (fora do range) não lança mais, não aparece em `window.onerror`/`unhandledrejection`, o app continua respondendo (`document.body` seguiu com conteúdo, não em branco), e o `.volume` real do elemento ficou corretamente em `1`, não `1.5` — confirma o clamp funcionando, não só a ausência de crash por acaso.
    - Todos os quatro bugs verificados numa única instância real do Electron (sem repetir a dificuldade de rodar uma segunda instância isolada, já documentada na v1.4.0) — os que precisavam de "outra pessoa" usaram uma segunda conta real via `curl` (member:joined) ou um stream falso só no dicionário certo (`remoteAudioStreams`, que `presence.onSync` nunca toca — diferente da tentativa com participante falso na v1.4.0, que morria em corridas de resync).
25. **Sistema de update, botão de entrar direto na transmissão, atalhos globais configuráveis, e correção de áudio "estourando", `v1.7.0`** — quatro pedidos do usuário.
    - **Repositório do GitHub tornado público** (era privado) — decisão do usuário depois de perguntado: sem isso, o instalador de cada release não seria baixável por quem não tem acesso ao repo (o que já era, sem querer, o caso até agora), e qualquer forma de checar/baixar update automaticamente precisaria ou de um token do GitHub embutido no app distribuído (risco real de segurança — qualquer um extrai do `.asar`) ou de a própria VPS virar um proxy de download de binário de ~115MB (infraestrutura nova, desproporcional ao pedido). Repo público resolve os dois de uma vez, sem nada disso.
    - **Sistema de update**: **não** é `electron-updater` — o instalador continua sem assinatura de código, então um auto-update silencioso levantaria o mesmo aviso do SmartScreen ("editor desconhecido") só que de um jeito mais confuso de explicar no meio de uma instalação automática. Em vez disso: `main/index.ts` checa `GET api.github.com/repos/.../releases/latest` (API pública do GitHub, sem token) a cada poucas horas (`UPDATE_CHECK_INTERVAL_MS`, mais uma checagem 5s depois de abrir — o app pode ficar dias na bandeja, v1.2.0, então só checar no boot deixaria passar reto por versões lançadas nesse meio tempo) e compara contra `app.getVersion()` (comparador de versão próprio, sem dependência nova — três números, não precisa de `semver`). Quando tem versão nova, manda um IPC (`updates:available`) pra um ícone novo no `TitleBar.tsx` (`UpdateIndicator`, `updatesStore.ts`) — clique baixa de verdade pra pasta de Downloads (`webContents.downloadURL` + `item.setSavePath`, sem diálogo de "Salvar como", barra de progresso nativa do Windows já aparece sozinha) e, quando termina, o mesmo ícone vira "Instalar" (`shell.openPath` no instalador baixado — a pessoa só confirma o NSIS que já conhece, não precisa mais ir procurar a versão nova manualmente). Erro no download cai num "abrir no navegador" (`shell.openExternal` da página do release) — sempre tem um jeito de conseguir a versão nova mesmo se o download direto falhar.
    - **Verificado ao vivo, de ponta a ponta, contra a API/infra reais**: rodou o app de dev com a versão forçada pra "1.0.0" (só pra esse teste) — o ícone `⬆ v1.6.0` apareceu sozinho, clicar baixou de verdade (confirmado o arquivo de ~115MB certinho na pasta de Downloads, mesmo tamanho do build original) e o ícone virou "Instalar v1.6.0". Não chegou a clicar em "Instalar" de propósito (abriria o instalador NSIS de verdade, risco desnecessário de mexer na instalação da própria máquina de teste só pra confirmar uma chamada de uma linha, `shell.openPath`).
    - **Botão de entrar direto na transmissão**: o roster de participantes (`VoicePanel.tsx`) já mostrava um indicador "● LIVE" pra quem está transmitindo, mas só texto — pra assistir, precisava rolar até o card do `GoLiveStreams` mais abaixo. Agora esse mesmo indicador é um botão de verdade (pra qualquer um que não seja você mesmo) — "● ENTRAR"/"● ASSISTINDO", chamando exatamente `watchStream`/`stopWatchingStream` (as mesmas ações já usadas pelo card, nada novo criado) — um segundo ponto de entrada mais direto, não substitui o card. Risco considerado baixo o bastante (reaproveita ações já comprovadas, só JSX condicional novo) pra não repetir um teste ao vivo dedicado.
    - **Atalhos globais configuráveis pra mutar/ensurdecer**: `globalShortcut` do Electron (não um `keydown` do renderer) de propósito — funcionam mesmo com a janela sem foco/minimizada na bandeja, o cenário mais comum de querer usar isso de verdade (jogando, app em segundo plano). `main/index.ts` registra via IPC (`shortcuts:set`) o que `settingsStore.ts` carrega do `localStorage` (mesma chave de sempre, ganha um campo `shortcuts: {mute, deafen}` novo, default `Control+Shift+M`/`Control+Shift+D`) assim que sabe quem logou; mudar em Configurações re-registra na hora. `SettingsModal.tsx` ganha um gravador de tecla (`ShortcutRecorder`) — clica, aperta a combinação, um conversor (`eventToAccelerator`) vira o formato Accelerator do Electron; exige pelo menos um modificador (Ctrl/Alt/Shift) de propósito, senão uma tecla "pelada" registrada globalmente sequestraria essa tecla de todo o resto do sistema enquanto o app estivesse aberto. Se o SO recusar o registro (já em uso por outro programa), a UI avisa na hora e não salva a preferência quebrada. Disparo real só chama `toggleMute`/`toggleDeafen` se `voiceStore.status === 'connected'` (senão é no-op silencioso) — os próprios sons de identificação (v1.5.0) já servem de confirmação audível de que o atalho funcionou, sem precisar de nenhum feedback visual novo.
    - **Verificado ao vivo**: gravação de tecla de verdade (Ctrl+Shift+K sintético via `KeyboardEvent` real, não `element.click()`) convertendo certinho pro Accelerator e **confirmando que `globalShortcut.register` aceitou de verdade no processo main** (a preferência só é salva no cliente depois de um `{ok: true}` do IPC — se não tivesse aceitado, não teria persistido). **Não verificado**: pressionar fisicamente a combinação registrada — não tem como simular um keypress *global* (nível de SO) via CDP, só eventos dentro da própria página; a ponta restante (`globalShortcut` disparando → IPC → `toggleMute`) é a mesma API padrão do Electron, código de uma linha em cada ponta, risco considerado baixo mas fica registrado como não clicado de verdade.
    - **Áudio "estourando" (clipping) na transmissão**: causa provável — o mixer do Windows soma várias fontes (jogo, música, e desde a v1.6.0 a própria voz da call, que passou a ficar audível de novo enquanto transmite) sem nenhum controle de nível antes de publicar; a soma passa de 0dBFS e corta digitalmente. `webrtc/audioLimiter.ts` novo — um `DynamicsCompressorNode` com ajuste de limiter de transmissão (ratio 16, ataque 3ms, não compressão suave) processa a track de áudio de sistema antes de publicar, tanto no Go Live (`goLiveStore.ts` — troca simples, LiveKit publica tracks direto) quanto no compartilhamento de tela com som (`voiceStore.ts` — mais delicado: a track processada precisa continuar dentro do MESMO `MediaStream` que a de vídeo, `stream.removeTrack`/`addTrack`, pra preservar o agrupamento por `msid` que o `MeshManager` depende desde a v1.4.0; a track crua continua viva por baixo, alimentando o grafo do Web Audio, e só é parada de verdade na hora de encerrar — rastreada à parte, `screenAudioLimiter` a nível de módulo, porque depois de removida do stream não tem mais como achar a referência dela de outro jeito). Limpeza (parar a track crua + fechar o `AudioContext`) adicionada em todo ponto que já encerrava a tela/transmissão antes (`stopScreenShare`, `stopGoLive`, troca de qualidade, `leave()` de ambas as stores).
    - **Verificado o mecanismo isoladamente, não o caminho completo**: a mesma limitação de hardware real documentada desde a v1.3.0 (loopback WASAPI falha com `NotReadableError` nessa máquina de teste específica) continua impedindo testar a captura de sistema de verdade neste ambiente. Em vez disso, `limitAudioTrack` foi testado direto contra um tom sintético (oscillator real) — confirma que devolve uma track **diferente** da original, **viva**, e que essa track processada carrega áudio de verdade (RMS não-zero medido nela via `AnalyserNode`) — prova que o pipeline (fonte → compressor → destino) funciona de ponta a ponta, mesmo sem poder validar contra a captura de loopback real nesta máquina.

26. **Dois bugs reais reportados testando o Go Live/tela compartilhada com gente de verdade, `v1.7.1`** — a primeira vez que o Go Live foi usado de ponta a ponta contra um LiveKit de verdade (nunca dava pra testar isso neste ambiente sem Docker), e os dois bugs só existiam nesse cenário real.
    - **Botão de "entrar na transmissão" simplesmente não aparecia pra quem já estava na call quando a transmissão começou antes**: causa raiz numa corrida real entre dois canais completamente diferentes. `goLiveStore.ts`'s `join()` registrava a `Presence` do canal `live:{id}` **depois** de `await liveRoom.connect(url, token, ...)` — uma operação de rede de verdade contra o LiveKit, nada instantânea. O backend (`go_live_channel.ex`, `handle_info(:after_join, ...)`) manda o `presence_state` inicial logo em seguida do reply do `channel.join()`, sem esperar nada do LiveKit — se a `Presence` do lado do cliente só existe depois do `liveRoom.connect()` terminar, esse push chega e é descartado em silêncio (nenhum listener ainda vinculado pra `"presence_state"`), e quem já estava ao vivo antes dessa pessoa entrar no canal nunca aparece como "ao vivo" pra ela (nem o botão do roster do `VoicePanel.tsx`, nem o card do `GoLiveStreams` — os dois dependem do mesmo `participants`/`live`), só se recupera se essa pessoa parar e começar a transmitir de novo (gera um `presence_diff` incremental, esse sim capturado a tempo). Corrigido movendo a criação da `Presence` (e seu `onSync`) pra **antes** até do `channel.join()`, mesmo padrão já usado (e comprovado) em `voiceStore.ts`.
    - **Tela compartilhada aparecendo em duplicado pra um usuário — um tile pequeno "fantasma" e um grande correto**: câmera e tela usam `replaceTrack(null)` pra desligar, de propósito, pra nunca precisar renegociar (ver o comentário no topo de `MeshManager.ts`) — mas `replaceTrack(null)` só deixa a track do **receptor** em estado `muted`, nunca `ended`, e só o evento `ended` disparava a limpeza (`onRemoteTrackEnded`, que apaga de `remoteCameraStreams`/`remoteScreenStreams`). Resultado: quem já estava na call vendo a câmera (ou tela) de alguém que depois desligou ficava com um tile pequeno travado no último frame pro resto da chamada — nunca sumia sozinho — e se essa mesma pessoa começasse a compartilhar a tela depois, o viewer via os dois ao mesmo tempo (o fantasma pequeno + o novo grande e correto). Corrigido em `voiceStore.ts`: o mesmo `presence.onSync` que já desambigua câmera-vs-tela ganhou uma segunda passada que limpa `remoteCameraStreams`/`remoteScreenStreams` quando a Presence diz que a câmera/tela dessa pessoa está desligada **e** a track em si já está `muted` de verdade (as duas condições, não só uma — só Presence sozinha arriscaria apagar um compartilhamento nôvo e válido ainda a caminho, já que a classificação em `onRemoteTrack` roda numa corrida parecida e só dispara uma vez por track; apagar cedo demais perderia esse compartilhamento pra sempre, sem nada que reponha depois).
    - **Verificado ao vivo o que dava pra verificar sem um segundo peer real (mesma limitação de sempre, Docker/LiveKit indisponível neste ambiente)**: instância real do Electron, conta de teste registrada e logada de verdade (`smoketest#9611`, banco local), `voiceStore.join()` chamado direto contra um canal de voz real — conectou normalmente (`status: "connected"`, mic capturado, `participants` com a própria entrada, sem lançar nada), confirmando que a nova passada de limpeza no `presence.onSync` não quebrou o caminho normal. `goLiveStore.join()` chamado em seguida no mesmo canal — o `channel.join()` e a `Presence` (já reordenados) completaram normalmente, com `participants` populado corretamente (a própria entrada, `live: false`) **antes** da falha esperada no `liveRoom.connect()` (sem LiveKit local pra conectar de verdade) — confirma que o novo código chega até lá e funciona pro trecho que dá pra rodar aqui. O que fica **não verificado** (mesma barreira estrutural de sempre): a corrida original de verdade — duas pessoas reais, uma já transmitindo quando a outra entra, contra um LiveKit real — e o cenário completo do tile fantasma com dois peers reais compartilhando tela/câmera. Banco de dev local resetado (`mix ecto.reset`) depois do teste, mesmo cuidado de sempre.

27. **Ver quem está em cada canal de voz sem entrar + barra fixa de voz, `v1.8.0`** — dois pedidos de qualidade de vida do usuário (itens 1 e 3 de uma lista sugerida).
    - **Ocupação dos canais de voz (backend + cliente)**: `voice_channel.ex` (`handle_info(:after_join)`) passa a fazer um segundo `Presence.track(self(), "server:{server_id}", user_id, %{voice_channel_id: ...})` — o mesmo tópico que `ServerChannel`/`presenceStore.ts` já usam pra "quem está online", mesma chave (`user_id`), só uma meta a mais. Rastreia o **processo do canal de voz**, então sair da call ou cair a conexão remove a meta sozinho. Sem `Presence.update` nesse tópico de propósito (mute/deafen não vão pra lá): cada update é um leave+join, e rajadas deles já causaram diffs fora de ordem antes. `presenceStore.ts` ganha `voiceOccupancy` (canal → quem está nele), `ChannelList.tsx` mostra os nomes embaixo de cada canal de voz em que você **não** está (no seu, continua a lista mais rica do `voiceStore`, com quem está falando/mutado). Sem filtro por permissão por destinatário — hoje ninguém tem canal escondido (`Channels.list_channels/1` também não filtra), mas precisa ser revisto se isso um dia mudar. 3 testes novos em `server_channel_test.exs`: quem entra no servidor depois vê quem já estava numa call; entrar na call avisa ao vivo quem está no servidor; sair da call tira a pessoa do canal mas não do online. 105 testes no total.
    - **Bug real achado no caminho**: o áudio da call (o `<audio>` do mic de cada pessoa) era renderizado **dentro** do `VoicePanel.tsx`, que só existe com o canal de voz selecionado — abrir um canal de texto no meio da call desmontava tudo e a pessoa parava de ouvir todo mundo (os outros continuavam ouvindo ela). Movido pra `components/CallAudio.tsx`, montado sempre pela `HomePage.tsx`. Tela/Go Live continuam no `VoicePanel` (sair daquela tela escondendo esses é o esperado).
    - **Barra fixa de voz**: `components/VoiceStatusBar.tsx`, no rodapé da lista de canais (prop `footer` nova do `ChannelList`), aparece enquanto houver call: "VOZ CONECTADA"/"CONECTANDO…", selo AO VIVO/🖥️ se você estiver transmitindo/compartilhando, nome do canal (clicar volta pra tela da call), e mutar/ensurdecer/SAIR.
    - **Verificado ao vivo**: Electron real + backend real + uma segunda conta real entrando/saindo da call por um cliente `phoenix` em Node (ocupação é só sinalização, não precisa de WebRTC nem de uma segunda janela do Electron). A segunda conta apareceu embaixo do canal de voz tanto quando já estava lá antes do servidor ser aberto quanto entrando ao vivo (~1s), e sumiu logo depois de sair. Barra: entrou na call clicando no canal, um stream de áudio sintético mudo foi injetado como participante falso, clique no canal de texto — `VoicePanel` sumiu e o `<audio>` continuou lá tocando o mesmo stream; mutar/ensurdecer pela barra funcionaram (ensurdecer mutou o elemento); clicar no nome do canal voltou pra call; SAIR levou voz e Go Live pra `idle`, tirou a barra e o elemento de áudio. **Depende de deploy do backend** na VPS pra ocupação funcionar em produção (sem ele, a lista só fica vazia — o resto funciona igual).

28. **Tela inicial de login em vez de cadastro, `v1.8.1`** — pedido do usuário, depois da limpeza de todas as contas de teste em produção (`ToraDosBurro.Release.wipe_users_except/2`). `App.tsx` abre em `login` (antes `register`, da época em que cadastro era o caminho normal e login só recuperação de sessão); o texto da `LoginPage.tsx` deixou de falar em "recuperar sessão". Verificado no Electron real: sem sessão, abre em "Entrar", e "Cadastre-se"/voltar trocam as telas nos dois sentidos.

29. **Ícone do app na identidade visual, `v1.8.2`** — pedido do usuário. O ícone ainda era o microfone roxo da FASE 11, de antes da identidade da v1.0.4. Agora é a mesma marca do login e da barra de título: quadrado volt (`#d6ff3f`) com o corte chanfrado no canto superior direito e um "T" void (`#0a0a10`). Gerado por `desktop/scripts/make-icon.mjs` (`node scripts/make-icon.mjs`), sem dependência nova: a marca é só geometria, então o script rasteriza cada tamanho do `.ico` separadamente, com as bordas alinhadas ao pixel pra continuar nítido em 16px, e grava as entradas no mesmo formato do ícone anterior (7 tamanhos, 16–256px, BMP 32bpp) — o formato mais compatível com o NSIS. Verificado: tamanhos pequenos conferidos ampliados sobre fundo escuro e claro; instalador gerado sem erro; ícone extraído de dentro do `.exe` do app, do instalador e do `resources/icon.ico` da bandeja, os três com a marca nova.

## Processo de desenvolvimento (regras do projeto, mantidas)

1. Não escrever código gigante de uma vez.
2. Implementar uma funcionalidade por vez.
3. Antes de implementar, explicar: objetivo, arquivos envolvidos, arquitetura, dependências, fluxo de dados.
4. Depois implementar.
5. Depois criar testes.
6. Depois explicar como executar.
7. Não inventar APIs ou bibliotecas.
8. Verificar compatibilidade entre versões.
9. Não adicionar dependências sem justificar.
10. Não criar microsserviços prematuramente.
11. Separar Control Plane / Media Plane / Data Plane.
12. Priorizar código simples, modular e sustentável.

Formato de resposta durante o desenvolvimento (a partir da FASE 1): Etapa atual → Objetivo → Arquitetura → Arquivos → Implementação → Banco de dados → Testes → Como executar → Como testar → Próxima etapa (só a próxima, nunca várias fases de uma vez).
