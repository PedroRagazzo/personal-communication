# Backend de produção (VPS via Dokploy, ver docs/roadmap.md) — sem domínio
# ainda, por isso http:// puro (não https://) e via IP direto. Usado só em
# `npm run dist:win` (electron-vite build lê .env.production automaticamente
# em modo produção); `npm run dev` continua apontando pro localhost:4000 via
# o default em src/renderer/src/services/{api,socket}.ts.
VITE_API_URL=http://2.28.229.48/api/v1
VITE_SOCKET_URL=ws://2.28.229.48/socket
